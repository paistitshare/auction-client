'use strict';

describe('auctionApp.newitpl module', function() {

  beforeEach(module('auctionApp.createVideoText'));

  describe('CreateVideoText controller', function(){

    it('should ....', inject(function($controller) {
      //spec body
      var createVideoTextCtrl = $controller('CreateVideoTextCtrl');
      expect(createVideoTextCtrl).toBeDefined();
    }));

  });
});