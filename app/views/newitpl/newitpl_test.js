'use strict';

describe('auctionApp.newitpl module', function() {

  beforeEach(module('auctionApp.create'));

  describe('view2 controller', function(){

    it('should ....', inject(function($controller) {
      //spec body
      var createPostCtrl = $controller('CreatePostCtrl');
      expect(createPostCtrl).toBeDefined();
    }));

  });
});