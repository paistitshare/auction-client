'use strict';

describe('auctionApp.auctions module', function() {

  beforeEach(module('auctionApp.posts'));

  describe('auctions controller', function(){

    it('should ....', inject(function($controller) {
      //spec body
      var postsCtrl = $controller('PostsCtrl');
      expect(postsCtrl).toBeDefined();
    }));

  });
});