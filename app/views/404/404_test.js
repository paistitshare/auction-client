'use strict';

describe('auctionApp.404 module', function() {

  beforeEach(module('auctionApp.404'));

  describe('404 controller', function(){

    it('should ....', inject(function($controller) {
      //spec body
      var notFoundCtrl = $controller('NotFoundCtrl');
      expect(notFoundCtrl).toBeDefined();
    }));

  });
});