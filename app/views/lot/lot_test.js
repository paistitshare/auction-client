'use strict';

describe('auctionApp.lot module', function() {

  beforeEach(module('auctionApp.post'));

  describe('lot controller', function(){

    it('should ....', inject(function($controller) {
      //spec body
      var postCtrl = $controller('PostCtrl');
      expect(postCtrl).toBeDefined();
    }));

  });
});